# test/fixtures/users.yml (Exemplo CORRIGIDO)
felipe:
  email: felipe@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User,'password') %>
  

maria:
  email: maria@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
 
admin_user:
  email: admin@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

  